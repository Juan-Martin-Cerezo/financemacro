{
  "permissions": {
    "allow": [
      "Bash(git init *)",
      "Bash(git add *)",
      "Bash(git commit -m 'feat: initial scaffolding — all backend services + frontend PWA dashboard *)"
    ]
  }
}
